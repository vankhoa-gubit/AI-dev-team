{
  "name": "ai-dev-team-harness",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Chat-native MCP delegation harness with Codex as leader and Antigravity as implementation worker",
  "bin": {
    "harness": "dist/cli.js",
    "harness-mcp": "dist/mcp-server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:compile": "tsc -p tsconfig.test.json",
    "test": "npm run test:compile && node --test dist-test/tests/mcp.test.js",
    "check": "npm run typecheck && npm test && npm run build",
    "smoke:real": "node scripts/smoke-real.mjs",
    "start": "node dist/mcp-server.js",
    "mcp": "node dist/mcp-server.js"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
